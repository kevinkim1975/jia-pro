# 의료기관 제안서 템플릿 - 재사용 가이드

## Boris Cherny 원칙
> "변하는 것과 변하지 않는 것을 명확히 분리하라"

---

## 1. 파일 분류

### 🔒 고정 파일 (그대로 복사)

```
medical-proposal-template/
│
├── src/
│   ├── types/
│   │   └── proposal.ts          ⭐ 슬라이드 타입 정의
│   │
│   ├── components/
│   │   ├── ui/
│   │   │   ├── BackgroundPattern.tsx   ⭐ 배경 패턴
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   └── sheet.tsx
│   │   │
│   │   ├── slides/
│   │   │   └── CoverSlide.tsx          ⭐ 표지 렌더러
│   │   │
│   │   ├── slide-renderers.tsx         ⭐ 모든 슬라이드 렌더러
│   │   ├── SlideWrapper.tsx            ⭐ 슬라이드 래퍼
│   │   ├── chart-slide.tsx             ⭐ 차트 컴포넌트
│   │   ├── topbar-header.tsx           ⭐ 헤더
│   │   ├── progress-footer.tsx         ⭐ 푸터
│   │   └── proposal-viewer.tsx         ⭐ 메인 뷰어
│   │
│   ├── app/
│   │   ├── globals.css                 ⭐ 색상/폰트 (Cleveland Clinic)
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   └── favicon.ico
│   │
│   ├── lib/
│   │   └── utils.ts
│   │
│   └── data/
│       └── (비워둠 - 새로 작성할 곳)
│
├── package.json
├── tsconfig.json
├── next.config.ts
├── postcss.config.mjs
├── eslint.config.mjs
├── .gitignore
└── README.md
```

### ✏️ 변경 파일 (새로 작성)

```
src/data/proposal-data.ts    ← 이것만 새로 작성!
```

---

## 2. 새 프로젝트 시작 방법

### Step 1: 템플릿 폴더 복사
```
C:\MyProject\crmp\
├── medical-proposal-template/   ← 템플릿 (원본)
├── jia-pro/                     ← 정이안한의원 (완성)
└── new-clinic/                  ← 새 프로젝트 (복사해서 생성)
```

### Step 2: 콘텐츠 MD 파일 준비
- `새한의원_마케팅전략_제안서.md` 파일을 프로젝트 폴더에 넣음

### Step 3: Claude Code와 작업
Claude Code에게 요청:
```
"새한의원_마케팅전략_제안서.md 파일을 읽고 
src/data/proposal-data.ts를 생성해주세요.
기존 jia-pro의 proposal-data.ts 형식을 참고하세요."
```

### Step 4: Vercel 배포
- GitHub에 push → Vercel 자동 배포

---

## 3. 수정이 필요한 곳 (선택적)

| 파일 | 수정 내용 | 필수 여부 |
|------|----------|:--------:|
| `src/components/topbar-header.tsx` | title 기본값 변경 | 선택 |
| `src/app/layout.tsx` | 메타데이터 (title, description) | 선택 |
| `package.json` | name 필드 | 선택 |

**참고**: 이것들은 수정 안 해도 동작함. proposal-data.ts만 있으면 됨.

---

## 4. proposal-data.ts 구조

```typescript
import type { Slide } from '@/types/proposal';

export const slides: Slide[] = [
  // 1. 표지
  {
    id: 'slide-1',
    pageNumber: 1,
    act: 1,
    content: {
      type: 'cover',
      title: '의원명',
      subtitle: '마케팅 전략 제안',
      date: '2026. 01',
      company: '호원앤컴퍼니',
    },
  },
  // 2. 목차
  {
    id: 'slide-2',
    pageNumber: 2,
    act: 1,
    content: {
      type: 'toc',
      items: [
        { act: 1, title: '상황', startPage: 3 },
        { act: 2, title: '위기', startPage: 6 },
        // ...
      ],
    },
  },
  // 3. ACT 간지
  {
    id: 'slide-3',
    pageNumber: 3,
    act: 1,
    content: {
      type: 'divider',
      act: 1,
      title: '상황',
      subtitle: 'Situation',
    },
  },
  // ... 나머지 슬라이드
];
```

---

## 5. 사용 가능한 슬라이드 타입

| 타입 | 용도 | 예시 |
|------|------|------|
| `cover` | 표지 | 제목, 날짜, 회사명 |
| `toc` | 목차 | ACT별 페이지 번호 |
| `divider` | ACT 간지 | ACT 제목, 부제 |
| `content` | 일반 콘텐츠 | 제목, 내용, 불릿, 강조 |
| `cards` | 3열 카드 | 핵심 수치, 강점 |
| `chart` | 차트 | line, bar, pie |
| `twoColumn` | 2열 비교 | 좌우 정보 비교 |
| `comparison` | Before/After | 변화 비교 |
| `quote` | 중앙 메시지 | 강조 문구 |
| `flowSteps` | 단계별 흐름 | 프로세스 설명 |
| `summary` | 요약 | 핵심 포인트 |
| `closing` | 마지막 | 감사, 연락처 |

---

## 6. 워크플로우 요약

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   템플릿      │     │   콘텐츠     │     │  Claude Code │
│   폴더 복사   │ ──▶ │   MD 작성    │ ──▶ │  data 생성   │
└──────────────┘     └──────────────┘     └──────────────┘
                                                 │
                                                 ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Vercel     │ ◀── │   GitHub     │ ◀── │   완성!      │
│   배포       │     │   Push       │     │              │
└──────────────┘     └──────────────┘     └──────────────┘
```

**핵심**: proposal-data.ts 하나만 새로 만들면 끝!
