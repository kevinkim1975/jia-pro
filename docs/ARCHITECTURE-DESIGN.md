# AutoMed Proposal Generator - 재사용 아키텍처 설계
## Boris Cherny 관점의 타입 안전한 제안서 시스템

---

## 1. 핵심 원칙

### Boris Cherny의 4가지 타입 시스템 원칙 적용

1. **컴파일 타임 오류 포착**: 잘못된 설정은 빌드 시점에 발견
2. **리터럴 타입 활용**: 문자열 대신 구체적인 타입 사용
3. **판별 유니온**: 슬라이드 타입별 안전한 분기
4. **불변성(readonly)**: 런타임 변경 방지

---

## 2. 레이어 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│  Layer 4: Content (Markdown)                            │
│  └─ proposal.md                                         │
├─────────────────────────────────────────────────────────┤
│  Layer 3: Configuration (TypeScript)                    │
│  └─ theme.ts, structure.ts, meta.ts                     │
├─────────────────────────────────────────────────────────┤
│  Layer 2: Generator (Scripts)                           │
│  └─ parse-md.ts, validate.ts                            │
├─────────────────────────────────────────────────────────┤
│  Layer 1: Core (Immutable)                              │
│  └─ types/, components/, lib/                           │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 변하는 것 vs 변하지 않는 것

| 구분 | 변하지 않는 것 (Core) | 변하는 것 (Per-Project) |
|------|----------------------|------------------------|
| **타입** | SlideType, 판별 유니온 | ActTitle (커스터마이징 가능) |
| **컴포넌트** | 슬라이드 렌더러, UI 컴포넌트 | 없음 |
| **디자인** | 레이아웃 구조 | 색상, 로고, 폰트 |
| **데이터** | 없음 | 모든 콘텐츠 |
| **패턴** | 패턴 종류 | 패턴 색상, 투명도 |

---

## 4. 디렉토리 구조 (최종)

```
clinic-name-pro/
├── config/                     # 🔧 프로젝트별 설정
│   ├── theme.ts                # 디자인 토큰
│   ├── structure.ts            # 6막 구조
│   └── meta.ts                 # 메타 정보
│
├── content/                    # 📝 콘텐츠 (MD)
│   ├── proposal.md             # 제안서 본문
│   └── assets/                 # 이미지 등
│       └── logo.png
│
├── scripts/                    # ⚙️ 생성 도구
│   ├── parse-md.ts             # MD → TS 파서
│   ├── validate.ts             # 타입 검증
│   ├── codegen.ts              # 코드 생성
│   └── generate.ts             # CLI 진입점
│
├── src/
│   ├── types/                  # 📐 Core 타입 (불변)
│   │   └── proposal.ts
│   │
│   ├── components/             # 🧩 Core 컴포넌트 (불변)
│   │   ├── slides/
│   │   ├── ui/
│   │   ├── SlideWrapper.tsx
│   │   ├── slide-renderers.tsx
│   │   ├── topbar-header.tsx
│   │   ├── progress-footer.tsx
│   │   └── proposal-viewer.tsx
│   │
│   ├── lib/                    # 🔧 유틸리티 (불변)
│   │   └── utils.ts
│   │
│   ├── data/                   # 📊 생성된 데이터
│   │   └── proposal-data.ts    # ← 자동 생성
│   │
│   └── app/                    # 📱 Next.js App
│       ├── layout.tsx
│       ├── page.tsx
│       └── globals.css
│
├── docs/                       # 📚 문서
│   └── ARCHITECTURE-DESIGN.md
│
├── package.json
├── tsconfig.json
└── README.md
```

---

## 5. 워크플로우

### 새 프로젝트 생성 시

```bash
# 1. 템플릿 복제
npx degit howon/automed-proposal clinic-name-pro
cd clinic-name-pro

# 2. 설정 수정
#    - config/theme.ts (색상, 패턴)
#    - config/meta.ts (의료기관 정보)

# 3. 콘텐츠 작성
#    - content/proposal.md

# 4. 데이터 생성
npm run generate

# 5. 개발 서버 실행
npm run dev
```

### 콘텐츠 수정 시

```bash
# 1. content/proposal.md 수정
# 2. 재생성
npm run generate
# 3. 자동 리로드 (HMR)
```

---

## 6. 타입 안전성 보장 (Boris Cherny 체크리스트)

| 체크 | 항목 | 구현 |
|------|------|------|
| ✅ | 컴파일 타임 오류 포착 | SlideContent 판별 유니온 |
| ✅ | 리터럴 타입 | ActNumber, SlideType |
| ✅ | 불변성 | readonly 모든 인터페이스 |
| ✅ | 타입 가드 | validateSlides() |
| ✅ | 설정 타입화 | ThemeConfig, ProposalMeta |
| ✅ | 생성 코드 타입 안전 | codegen.ts 타입 검증 |

---

*이 문서는 Boris Cherny의 "Programming TypeScript" 원칙을 기반으로 설계되었습니다.*
